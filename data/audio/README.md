# Audio Data Directory

This directory contains audio files:

- `raw/` - Downloaded original audio files (MP3)
- `normalized/` - Normalized audio files (16kHz mono WAV)

These files are gitignored. Run the pipeline to populate.
